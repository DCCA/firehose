# Tasks: Add User Search by Email

## Checklist

- [x] 1. Add `email` query parameter parsing to the users controller
- [x] 2. Add case-insensitive email lookup query to the user repository
- [x] 3. Write tests for email search (match, no match, case-insensitive)
- [x] 4. Write test confirming `GET /users/:id` is unchanged
